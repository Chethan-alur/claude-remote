# Project-wide Gradle settings.
# AndroidX is required by AGP 8 and by every dependency this app uses.
android.useAndroidX=true
# Give the Gradle daemon enough heap for the Kotlin + Compose compilers.
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
kotlin.code.style=official
